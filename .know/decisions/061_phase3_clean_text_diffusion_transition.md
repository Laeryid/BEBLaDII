<!-- created: 2026-07-22 -->
# ADR 061: Переход на диффузию чистых текстов без ChatML-тегов (Phase 3 Clean Text Transition)

## Context and Problem

В Phase 3 (каноническая безусловная диффузия, ADR 060) обучение DUS проводилось на датасете `reasoning`, где каждая фракция кодировалась в формате ChatML (`<|im_start|>user\n...<|im_end|>\n<|im_start|>assistant\n<|thought|>\n...`). 

Глубокий анализ чекпоинтов на 10 000 шагов (`step_9995.pth`) и 18 000 шагов (`step_17995.pth`) вскрыл следующие фундаментальные проблемы:

1. **Паразитный оверхед емкости DUS:** До 40% ёмкости латентного пространства 40-слойного DUS тратилось на дублирование и ротацию статических системных префиксов ChatML.
2. **Рассогласование латентных пространств с декодером (System 1 vs System 2):** `ModernLatentDecoder` (Фаза 2) обучался на чистом тексте без служебных ChatML-тегов. Когда диффузионные векторы из DUS передавались в декодер, декодер воспринимал ChatML-структуры как шум и генерировал повторы (`-prefix`, `Search`, `Nest`).
3. **Деградация 18K шага по сравнению с 10K шагом:** Анализ `metrics_history` и локальных тестов показал, что чекпоинт на 10 000 шагов (`step_9995`) превосходит 18 000 шаг:
   - `step_9995`: при диффузии из чистого шума $t=1.0 \to 0.0$ уверенно держит $CosSim \approx 0.4286$ и сходится к релевантным смысловым токенам.
   - `step_17995`: деградировал на высоком шуме $t > 0.7$ (`cos_sim_t_high` рухнул до 0.0000 / 0.4764), скатываясь в аттрактор.

## Decisions Made and Lessons Learned

- **What was tried and didn't work:**
  - Обучение канонической диффузии на ChatML-форматированных текстах.
  - Попытки динамического усечения маски ChatML-тегов перед передачей вектора в декодер на инференсе (костыли на инференсе не компенсируют пространственный сдвиг многообразия).

- **Successful Solution (Best Practice):**
  1. **Переход к Clean Text Diffusion (при перезапуске в Kaggle):** При возобновлении обучения Phase 3 (после обновления квоты Kaggle) DataLoader предварительно вырезает служебные ChatML-теги. DUS обучается исключительно на чистых текстовых репликах рассуждений (`assistant` reasoning).
  2. **100% Геометрическое совпадение пространства:** Латентные векторы Qwen для чистых текстов будут идеально совпадать с пространством, на котором обучался `ModernLatentDecoder` (Фаза 2), устраняя разрыв между System 1 и System 2.
  3. **Канонический базовый чекпоинт `step_9995`:** До нового перезапуска обучения официальным базовым чекпоинтом Phase 3 объявляется `planB_phase3_checkpoints_phase3_step_9995.pth` (10 000 шагов).

## Impact

- **Позитивный:** Декодирование диффузионных векторов станет прямым, чистым и осмысленным без вырезания тегов.
- **Позитивный:** 40 слоев DUS полностью сфокусируются на истинной семантике языка без затрат ресурсов на служебный синтаксис.
- **Принятое ограничение:** Требуется повторный запуск обучения Phase 3 в Kaggle после обновления недельной квоты GPU.
