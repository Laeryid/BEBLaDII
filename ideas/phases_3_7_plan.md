# План обучения: Фазы 3–7

> Статус: обсуждение / предварительный план (29.05.2026)
> Контекст: фаза 2 идёт на TPU (~4 суток), фазы 3–7 планируются на локальной машине.

---

## Общий контекст

- **Токенизатор**: штатный `Qwen/Qwen2.5-7B-Instruct` + 3 спецтокена (`<|thought|>`, `<|im_start|>`, `<|im_end|>`). Словарь ~152k токенов.
- **Диффузионное пространство**: выход `mu` из mu-VAE (1024-dim). mu-VAE обучается в Phase 2 вместе с latentBERT.
- **Локальная машина**: Intel Xe Iris (интегрированная), 15 ГБ RAM, CPU inference.
- **Стратегия памяти**: «загрузил → посчитал → сохранил → выгрузил», никогда не держать два тяжёлых компонента одновременно.

---

## Phase 3: Output Projector

**Цель**: обучить проектор переводить внутреннее состояние слоя 40 latentBERT в диффузионное пространство (1024-dim → 1024-dim).

### Датасет
- Прогнать весь словарь (~152k токенов) поштучно через **замороженный mu-VAE** → сохранить `zmu.npy` (152k × 1024).
- Прогнать весь словарь через **замороженный latentBERT** (слой 40) → сохранить `h40.npy` (152k × 1024).
- Однотокенный инференс: seq_len=1, attention = O(1), ожидаемое время на CPU ~30–60 мин.

### Обучение
```
h40[i] → Output Projector → zmu[i]
Loss: MSE или Centered Cosine (см. ADR 015/016)
```
- Заморожено: latentBERT, mu-VAE.
- Обучается: только Output Projector.
- Ожидаемое время: ~5–15 мин.

### Обоснование выбора датасета
Проектор учит геометрическое преобразование пространств, а не паттерны последовательностей.
Словарь из 152k токенов обеспечивает достаточное покрытие семантического многообразия.
OOD-риск (однотокенное обучение vs. мультитокенный инференс) — проверить постфактум через
анализ распределения h_40 на реальных последовательностях.

---

## Phase 4: Decoder Bridge (Latent Decoder)

**Цель**: обучить мост переводить финальное 1024-dim представление обратно в 3584-dim пространство Qwen2.5.

### Датасет
- Использовать `zmu.npy` из Phase 3 (уже готов).
- Загрузить только матрицу `lm_head.weight` из `Qwen2.5-7B-Instruct` (~1 ГБ bf16).

### Обучение
```
zmu[i] → Decoder Bridge → 3584-dim → frozen LM-head → logits
Loss: CrossEntropy(logits, token_id=i)
```
- Заморожено: mu-VAE, LM-head (150k оригинальных строк).
- Обучается: Decoder Bridge + 3 строки спецтокенов в LM-head.
- Метрика качества: **top-1 / top-5 accuracy** по всему словарю.
- Ожидаемое время: ~15–30 мин.

### Примечание по LM-head
- Используем стандартную LM-head Qwen2.5-7B. Расширения словаря нет.
- Альтернатива для экономии памяти: LM-head от Qwen2.5-0.5B (896-dim, ~0.27 ГБ),
  тогда Bridge выдаёт 896-dim. Словарь идентичен, нужен бенчмарк top-1 accuracy.

---

## Диагностика латентного пространства (между Phase 2 и Phase 3)

Сразу после завершения Phase 2 — проверить диффузионное пространство на пригодность
для прямого индексирования (возможный пропуск Phase 5/6).

### Метрики

| Метрика | Порог «OK» | Порог «нужен маппер» |
|---------|-----------|----------------------|
| Participation Ratio (PR) | > 0.5 | < 0.2 |
| Explained Variance PC1 | < 10% | > 20% |
| Средняя попарная косинусная схожесть (μ_cos) | < 0.1 | > 0.3 |
| Norm CV | < 0.2 | > 0.5 |
| Hubness skewness | < 1.5 | > 3.0 |

### Процедура
```python
# Прогнать весь словарь через frozen mu-VAE, взять mu (без сэмплирования)
# Посчитать метрики на полученных 152k × 1024 векторах
```
Этот шаг бесплатен — `zmu.npy` всё равно нужен для Phase 3.

---

## Phase 5: CLM Pooling

**Статус**: возможно пропустить.

Если метрики выше зелёные — mean pooling латентных токенов достаточен для формирования
CLM-запроса. Фаза нужна только если нужен специализированный механизм сжатия последовательности.

---

## Phase 6: CLM Index Mapper

**Статус**: возможно пропустить.

Если PR > 0.5 и μ_cos < 0.1 — пространство уже ANN-friendly, дополнительный маппинг не нужен.
Решение принимается по результатам диагностики.

---

## Phase 7: Confidence Head

**Цель**: обучить голову уверенности на синтетических данных как геометрический «датчик энтропии».

### Датасет
- Синтетический: чистые векторы, интерполяции между ними, зашумлённые векторы.
- Метки: степень «кристаллизованности» (0 = шум, 1 = чистый вектор).

### Обучение
- Полностью локально, CPU.
- Маленький MLP поверх 1024-dim входа.
- Ожидаемое время: ~10 мин.

---

## Временные оценки (локальная машина, CPU)

| Фаза | Тяжёлый шаг | Оценка |
|------|-------------|--------|
| Диагностика пространства | zmu.npy + метрики | ~30–60 мин |
| Phase 3 — генерация датасета | latentBERT inference | ~30–60 мин |
| Phase 3 — обучение проектора | на .npy парах | ~5–15 мин |
| Phase 4 — обучение Bridge | zmu.npy + lm_head | ~15–30 мин |
| Phase 7 — Confidence Head | синтетика | ~10 мин |
| **Итого** | | **~2–3 часа** |
